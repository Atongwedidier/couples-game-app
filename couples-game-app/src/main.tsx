import React from 'react';
import ReactDOM from 'react-dom/client';
import OnlineCouplesGamePlatform from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OnlineCouplesGamePlatform />
  </React.StrictMode>,
);
